\section{Introduction}
\label{sec:intro}
In this protocol, the multivariate analysis is applied to the selection of $B_s^0 \to \psi \left(2S\right)\, K_s^0$ events. The $B_s^0$ signal is initially not visible due to the large background contribution. To make the $B_s^0$ signal visible, there are several methods used, like \textit{Feature Selection}, \textit{k-fold cross validation} or \textit{decision trees}, which are introduced in the next \autoref{sec:theory}.